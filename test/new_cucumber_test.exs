# This file demonstrates the new cucumber architecture
# In a real app, the compile_features! call would go in test_helper.exs

# Compile cucumber features into test modules
Cucumber.New.compile_features!()

# The tests are now compiled and will run as part of ExUnit
